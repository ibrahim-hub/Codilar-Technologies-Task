import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Adjust the path as necessary

ReactDOM.render(
  <h1>Test</h1>,

  document.getElementById('app') // Ensure you have an element with id "app" in your HTML
);
